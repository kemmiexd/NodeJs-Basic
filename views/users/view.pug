extends ../layouts/kemmie.pug

block content

    h1 View User

    table.table
        tr
            th Name:
            th Phone:
            th Email:
            th Password:

        tr
            td=user.name
            td=user.phone
            td=user.email
            td=user.password

    a.btn.btn-danger(href="/users") Trở về doanh trại